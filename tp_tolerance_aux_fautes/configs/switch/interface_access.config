# Port access generique (PC, Serveur)
# Remplacer <INTERFACE> par le port (ex: FastEthernet0/1, FastEthernet0/2)
enable
configure terminal
interface <INTERFACE>
    switchport mode access
    no shutdown
exit
end
write memory
