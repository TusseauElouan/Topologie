# Port uplink vers le routeur
# Remplacer <INTERFACE> par le port (ex: GigabitEthernet0/2)
enable
configure terminal
interface <INTERFACE>
    switchport mode access
    no shutdown
exit
end
write memory
