# Port trunk inter-switch (lien de redondance Switch1 <-> Switch2)
# Remplacer <INTERFACE> par le port (ex: GigabitEthernet0/1)
enable
configure terminal
interface <INTERFACE>
    switchport mode trunk
    no shutdown
exit
end
write memory
